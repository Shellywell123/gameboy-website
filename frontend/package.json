{
  "name": "threejs",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "start": "yarn build && parcel index.html",
    "prebuild": "rm -rf dist && mkdir -p dist/assets && cp -R src/assets dist",
    "build": "parcel build index.html"
  },
  "dependencies": {
    "detect-gpu": "0.0.40",
    "three": "^0.134.0",
    "three-orbitcontrols": "2.102.1",
    "threex": "^1.0.0"
  },
  "devDependencies": {
    "@babel/core": "7.2.0",
    "jest": "^27.3.1",
    "parcel-bundler": "^1.6.1",
    "sass": "^1.49.7",
    "typescript": "^4.4.4"
  },
  "keywords": []
}
